name: Keep Course Services Awake

on:
  schedule:
    - cron: "17 12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping course health endpoint
        run: |
          curl --fail --show-error --silent --max-time 60 https://tiforself.onrender.com/api/health
